package ServerHandler;

import static org.junit.Assert.*;

import org.json.simple.JSONArray;
import org.json.simple.JSONObject;
import org.junit.Test;


public class HeatMapHandlerTest{

	static int numberOfRegions = 4;
    @Test
    public void testGetBinsfromDB() throws Exception {
        /*HeatMapHandler testheatmap = new HeatMapHandler();
        
        JSONArray testRegionBins = testheatmap.getBinsFromDB();
        int total=0;
        
        assertNotNull(testRegionBins);
        
     
        if(testRegionBins.isEmpty())
        {
        	System.out.println("No regions resturned. Heat map will not be displayed");
        }
        else
        {
        		System.out.println(testRegionBins.size());
        		assertEquals(numberOfRegions, testRegionBins.size());
        		JSONObject testJsonObject = (JSONObject)testRegionBins.get(0);
        		assertEquals("Trinity", (String) testJsonObject.get("region_name"));
        	}*/
   		}
       }

    
    
